seed: 0
device: auto
output_dir: outputs/feature_run
scenario:
  data:
    backend: manifest
    path: data/manifest.csv
    root: null
train:
  epochs: 1
  batch_size: 32
  num_workers: 0
  optimizer:
    type: adamw
    lr: 1.0e-4
    weight_decay: 1.0e-4
method:
  num_classes: 2
  detector_cfg:
    backbone:
      type: identity
      in_dim: 64
